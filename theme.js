import { extendTheme } from "@chakra-ui/react";

const colors = {
  base: "#0A192F",
  base2: "#60E8CD",
  text1: "#8892B0",
  text2: "#CBD6F6",
};

const fonts = {
  body: "alef",
};

export const theme = extendTheme({ colors, fonts });
